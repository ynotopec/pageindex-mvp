[project]
name = "pageindex-mvp"
version = "0.1.0"
description = "Minimal document Q&A Streamlit app using VectifyAI/PageIndex Collection API."
requires-python = ">=3.10"
dependencies = [
  "streamlit>=1.30.0",
  "openai-agents[litellm]>=0.2.0",
  "pageindex @ git+https://github.com/VectifyAI/PageIndex.git@dev",
]

[tool.uv]
package = false
